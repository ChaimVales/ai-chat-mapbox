# Distance Measurement Feature

מודד מרחק בין נקודות במפה (haversine - חישוב על כדור הארץ).

## איך להפעיל
ב-MapView.tsx:
```tsx
import { DistanceTool } from '../../features/distance-measurement/feature';
<DistanceTool map={mapInstance} />
```

## איך להסיר
מחק תיקייה.
